To edit the file context by using sed editor

for example:

search and replace
delete the context
adding the lines

sed 's/root/tree' file     (it is changing the output of the file)

-i (it is changing on the file)
 g (is for the global)
 -e (is for the entire line).
